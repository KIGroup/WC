<?xml version="1.0" encoding="UTF-8"?>
<Export generator="Cache" version="25">
<Class name="WC.Utils.Email">
<Abstract>1</Abstract>
<TimeCreated>63370,70076.016024</TimeCreated>

<Method name="Send">
<ClassMethod>1</ClassMethod>
<FormalSpec>email:%String,subject:%String,msg:%String</FormalSpec>
<ReturnType>%Status</ReturnType>
<Implementation><![CDATA[
	set status = $$$OK
	
	try
	{
		set mail=##class(%Net.MailMessage).%New()
    	set mail.From = $GET(^Settings("WC","EmailFrom"))
    	set mail.Subject = subject
    	set mail.Charset = "utf-8"
    	set mail.IsHTML = 1
    	set mail.ContentType = "text/html"
    	do mail.TextData.Write(msg)
    	
    	if ($CLASSNAME(email) = "%Library.ListOfDataTypes"){
    		for i=1:1:email.Count(){
	    		do mail.To.Insert(email.GetAt(i))
    		}
    	}
    	else{
	    	do mail.To.Insert(email)
    	}
    	
    	set auth=##class(%Net.Authenticator).%New()
  		set auth.UserName = $GET(^Settings("WC","EmailSMTPUSER"))
  		set auth.Password = $GET(^Settings("WC","EmailSMTPPASSWORD"))
  	
  		set smtp = ##class(%Net.SMTP).%New()
    	set smtp.smtpserver = $GET(^Settings("WC","EmailSMTPSERVER"))
    	set smtp.port = $GET(^Settings("WC","EmailSMTPPORT"))
  		set smtp.authenticator = auth
  	
    	$$$THROWONERROR(status, smtp.Send(mail))
	}
	catch (ex){
		set status = ex.AsStatus()
  	}
  	
	quit status
]]></Implementation>
</Method>
</Class>
</Export>
